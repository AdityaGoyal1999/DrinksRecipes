import { StyleSheet, Text, SafeAreaView, View } from 'react-native'
import React from 'react'

export default function profile() {
  return (
    <SafeAreaView>
        <View className="mx-4">
            <Text className="text-[35px] font-bold">Profile</Text>
         </View>
    </SafeAreaView>
  )
}

const styles = StyleSheet.create({})